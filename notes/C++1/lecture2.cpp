#include <iostream>

using namespace std; // If anything I type you dont recognize smth, search in standard


int main(int argc, char * argv[])
{
    int age = 39;
    string first = "Dave";
    std::string second = "Pilotes";

    std::cout << first + " " + second << std::endl; // Different endlines for different OS

    return 0;
}